[project]
name = "espclock-tools"
version = "0.1.0"
description = "Pinned host-side development tools for the ESPClock firmware"
requires-python = ">=3.11"
dependencies = [
    "platformio==6.1.19",
]

[tool.uv]
package = false

